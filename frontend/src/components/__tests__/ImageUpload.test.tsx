describe('deleteExpiredImages', () => {
    it('removes images that have passed expiration', async () => {
        // 1. Insert mock images into a test DB with various expiration times
        // 2. Call deleteExpiredImages()
        // 3. Assert that only valid images remain
    });
});
